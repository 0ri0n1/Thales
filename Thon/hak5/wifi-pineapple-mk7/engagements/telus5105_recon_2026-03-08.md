# TELUS5105 Neighbor Network Recon Report

**Date:** 2026-03-08
**Operator:** Thon (DRIFTER lens — Discovery)
**Authority:** Eddie (Sovereign/Principal)
**Classification:** RECON ONLY — Passive wireless observation
**Equipment:** WiFi Pineapple Mark 7 (wlan1mon), Channel 11
**Governance:** WARDEN — Authorized neighbor probe per Eddie's directive

---

## Mission

Identify devices connected to the TELUS5105 neighbor network via passive wireless observation on Channel 11.

## Target

| BSSID | ESSID | Security | Signal | Notes |
|---|---|---|---|---|
| FC:2B:B2:F7:C8:C2 | TELUS5105 | WPA2 PSK | -85 dBm | Actiontec CPE (TELUS-issued). 211 beacons, 0 IVs |

**Infrastructure note:** Standard Actiontec hardware (FC:2B:B2 = Actiontec Electronics). Same OUI block as TELUS5150. Zero data IVs — no encrypted client traffic observed, but tcpdump revealed probe response activity.

---

## Findings: Connected Devices

### TELUS5105 (FC:2B:B2:F7:C8:C2) — 0 clients observed

No associated clients detected in airodump-ng. No client data frames seen in tcpdump.

---

## Additional Observations

### Probe Response Activity
| Target MAC | Vendor | Activity |
|---|---|---|
| EA:39:D8:D1:F2:6B | Locally Administered | Multiple Probe Responses (6+) sent by TELUS5105 AP. A device with a randomized MAC is actively searching for this network. Likely a phone or laptop that was previously connected. |

---

## Analysis (DRIFTER → VANGUARD)

1. **Silent network.** Zero data IVs and zero associated clients. TELUS5105 is broadcasting beacons but has no active 2.4GHz clients at the time of capture.

2. **Device seeking reconnection.** EA:39:D8 is probing for TELUS5105 — the AP is responding with Probe Responses — but the device is not completing association. This could indicate the device is out of range for reliable data transfer, has changed WiFi passwords, or the device is simply scanning while connected to a different network.

3. **Randomized MAC makes tracking difficult.** The probing device uses a locally administered MAC, which changes periodically. Without additional correlation (timing, signal strength patterns), this device cannot be reliably tracked across sessions.

4. **Same hardware as TELUS5150.** Shares the FC:2B:B2 Actiontec OUI — identical hardware model. If TELUS5150 is the same physical location as TELUS5105 (dual-band), they would represent a single subscriber. However, their different signal strengths (-54 dBm vs -85 dBm) suggest they are physically separate locations.

---

## Reproduction Steps

```bash
# 1. Set Pineapple monitor to Channel 11
sshpass -p <PASS> ssh root@172.16.42.1 "iw dev wlan1mon set channel 11"

# 2. Run 60s airodump-ng capture on Channel 11
sshpass -p <PASS> ssh root@172.16.42.1 \
  "rm -f /tmp/ch11-01.* 2>/dev/null; \
   airodump-ng wlan1mon --channel 11 --write /tmp/ch11 --output-format csv >/dev/null 2>&1 & \
   sleep 60; killall airodump-ng 2>/dev/null; sleep 2; cat /tmp/ch11-01.csv"

# 3. Filter results for target BSSID
# AP section: grep for FC:2B:B2:F7:C8:C2
# Client section: check BSSID column for match

# 4. Targeted tcpdump for packet-level detail
sshpass -p <PASS> ssh root@172.16.42.1 \
  "iw dev wlan1mon set channel 11 && tcpdump -i wlan1mon -e -c 500 2>&1" \
  | grep -i "fc:2b:b2:f7:c8:c2" | grep -v Beacon

# 5. OUI lookup
grep -i "PREFIX" /usr/share/nmap/nmap-mac-prefixes
```

**Time required:** ~90 seconds (60s capture + 30s tcpdump)
**No offensive tools used.** Passive observation only.

---

*Report generated by Thon — DRIFTER lens (Discovery)*
*Authority: Eddie — Sovereign principal*
*Classification: RECON ONLY — Zero offensive operations conducted*
