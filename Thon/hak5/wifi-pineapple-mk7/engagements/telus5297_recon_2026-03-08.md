# TELUS5297 Neighbor Network Recon Report

**Date:** 2026-03-08
**Operator:** Thon (DRIFTER lens — Discovery)
**Authority:** Eddie (Sovereign/Principal)
**Classification:** RECON ONLY — Passive wireless observation
**Equipment:** WiFi Pineapple Mark 7 (wlan1mon), Channel 11
**Governance:** WARDEN — Authorized neighbor probe per Eddie's directive

---

## Mission

Identify devices connected to the TELUS5297 neighbor network via passive wireless observation on Channel 11.

## Target

| BSSID | ESSID | Security | Signal | Notes |
|---|---|---|---|---|
| 9C:1E:95:F1:B4:A2 | TELUS5297 | WPA2 PSK | -89 dBm | Actiontec CPE (TELUS-issued router). 292 beacons, 43 IVs — significant data traffic |

**Infrastructure note:** Router internal MAC 9C:1E:95:F1:B4:A0 observed sending IGMP multicast (01:00:5e:00:00:01), confirming active network management. This is a standard TELUS residential Actiontec deployment.

---

## Findings: Connected Devices

### TELUS5297 (9C:1E:95:F1:B4:A2) — 2 clients observed

| Client MAC | Vendor | Packets | Signal | Device Assessment |
|---|---|---|---|---|
| D8:1F:12:A4:1F:29 | **Tuya Smart** | 82 | -82 dBm | **Smart home IoT device (Tuya platform).** Extremely active — 82 packets in 60s with continuous broadcast and unicast data frames. Tuya is a major IoT cloud platform powering thousands of white-label smart devices (plugs, bulbs, cameras, sensors). This volume suggests a device actively streaming data (possibly a camera) or performing frequent cloud check-ins. |
| 5C:C1:D7:D7:A3:D4 | **Samsung Electronics** | 12 | -76 dBm | Samsung device — likely a smartphone, tablet, or smart TV. Moderate activity (12 packets) suggests active use during capture window. |

---

## Analysis (DRIFTER → VANGUARD)

1. **Hyperactive Tuya device.** D8:1F:12 generated 82 packets in 60 seconds — by far the most active single client observed across all neighbor networks. Tuya devices are known for frequent cloud communication. This traffic volume is consistent with a WiFi camera streaming to cloud, or a hub device coordinating multiple Tuya sensors.

2. **Tuya security implications.** Tuya devices have a documented history of security vulnerabilities including hardcoded credentials, unencrypted local API calls, and cloud key extraction. The high packet rate makes this device particularly interesting for traffic analysis.

3. **Samsung presence confirms residential use.** The Samsung device (5C:C1:D7) at -76 dBm is closer than the AP itself (-89 dBm), suggesting it's physically between our position and the router. Likely a phone or TV in active use.

4. **Most active neighbor network.** With 43 IVs and 94 total client packets, TELUS5297 has the highest data throughput of all 5 TELUS neighbor networks on Channel 11.

---

## Reproduction Steps

```bash
# 1. Set Pineapple monitor to Channel 11
sshpass -p <PASS> ssh root@172.16.42.1 "iw dev wlan1mon set channel 11"

# 2. Run 60s airodump-ng capture on Channel 11
sshpass -p <PASS> ssh root@172.16.42.1 \
  "rm -f /tmp/ch11-01.* 2>/dev/null; \
   airodump-ng wlan1mon --channel 11 --write /tmp/ch11 --output-format csv >/dev/null 2>&1 & \
   sleep 60; killall airodump-ng 2>/dev/null; sleep 2; cat /tmp/ch11-01.csv"

# 3. Filter results for target BSSID
# AP section: grep for 9C:1E:95:F1:B4:A2
# Client section: check BSSID column for match

# 4. Targeted tcpdump for packet-level detail
sshpass -p <PASS> ssh root@172.16.42.1 \
  "iw dev wlan1mon set channel 11 && tcpdump -i wlan1mon -e -c 500 2>&1" \
  | grep -i "9c:1e:95:f1:b4:a2" | grep -v Beacon

# 5. OUI lookup
grep -i "PREFIX" /usr/share/nmap/nmap-mac-prefixes
```

**Time required:** ~90 seconds (60s capture + 30s tcpdump)
**No offensive tools used.** Passive observation only.

---

*Report generated by Thon — DRIFTER lens (Discovery)*
*Authority: Eddie — Sovereign principal*
*Classification: RECON ONLY — Zero offensive operations conducted*
